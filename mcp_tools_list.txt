================================================================================
MCP TOOLS LIST
================================================================================

Total Tools: 15

1. get_workflow_validation_status
   Description: Get validation status for workflows. Shows errors and warnings for workflows that failed validation during startup.
   Parameters:
     - workflowName (optional) [string]: Optional workflow name to get status for. If not provided, returns all workflows.
     - failedOnly (optional) [['boolean', 'null']]: Include only failed workflows (default: false)

2. run_batch_kusto_query
   Description: Execute multiple Kusto queries in batch with execution metrics for each query. Supports configurable concurrency and fail-fast or best-effort modes.
   Parameters:
     - queries (optional) [array]: Array of query specifications. Each must have either query_name or custom_query, plus optional cluster, database, parameters, and query_id.
     - failFast (optional) [['boolean', 'null']]: Stop execution on first error if true (default: false)
     - maxConcurrency (optional) [['integer', 'null']]: Maximum number of queries to execute in parallel (default: 5, max: 20)

3. clear_investigation_context
   Description: Clear the current investigation context.

4. get_query_schema
   Description: Get schema information for a specific Kusto query including parameters and usage examples.
   Parameters:
     - queryName (required) [string]: Query name to get schema for
     - includeInternal (optional) [['boolean', 'null']]: Include internal queries (default: false)

5. check_kusto_health
   Description: Check Kusto cluster connectivity and health. Validates authentication, connectivity, and query execution.
   Parameters:
     - cluster (optional) [string]: Cluster URL to check (optional, uses default if not specified)
     - database (optional) [string]: Database to test queries against (optional, uses Fa if not specified)

6. get_current_investigation
   Description: Get the current investigation context showing active incident folder and metadata.

7. run_workflow
   Description: Execute a workflow with provided parameters.
   Parameters:
     - workflowName (required) [string]: Name of the workflow to execute
     - parameters (optional) [string]: Workflow parameters as JSON object

8. get_workflow_schema
   Description: Get detailed schema for a specific workflow including parameters and steps.
   Parameters:
     - workflowName (required) [string]: Workflow name to get schema for
     - includeSteps (optional) [['boolean', 'null']]: Include detailed step information (default: false)

9. resolve_to_node_id
   Description: Resolve various identifiers (container ID, VM ID, etc.) to Azure node ID. REQUIRES investigation context - call create_incident_folder first.
   Parameters:
     - containerId (optional) [string]: Container ID to resolve (optional)
     - vmId (optional) [string]: VM ID to resolve (optional)
     - armResourceId (optional) [string]: ARM resource ID to resolve (optional)
     - correlationId (optional) [string]: Correlation ID to resolve (optional)
     - subscriptionId (optional) [string]: Subscription ID to resolve (optional)
     - tenantId (optional) [string]: Tenant ID to resolve (optional)
     - resourceGroup (optional) [string]: Resource group name (optional, used with subscriptionId and vmName)
     - vmName (optional) [string]: VM name (optional, used with subscriptionId and resourceGroup)
     - incidentTime (optional) [string]: Override incident time for query window (ISO 8601 format, optional)
     - cluster (optional) [string]: Kusto cluster (default: 'azcore.centralus')
     - database (optional) [string]: Kusto database (default: 'Fa')

10. run_kusto_query
   Description: Execute a named Kusto query template or custom KQL query against Azure Data Explorer. You must provide EITHER queryName OR customQuery, not both.
   Parameters:
     - queryName (optional) [string]: Name of query from resources/queries directory (without .kql extension). Mutually exclusive with customQuery.
     - customQuery (optional) [string]: Custom KQL query to execute. Mutually exclusive with queryName.
     - parameters (optional) [object]: Query parameters as JSON object
     - cluster (optional) [string]: Kusto cluster URL (optional)
     - database (optional) [string]: Kusto database name (optional)
     - writeToFile (optional) [['boolean', 'null']]: Explicitly write results to file (automatic when investigation context exists)
     - writeChart (optional) [['boolean', 'null']]: Generate PNG chart from query results (requires investigation context)
     - outputFilename (optional) [string]: Custom filename for output (optional)
     - outputFormat (optional) [string]: Output format: 'json' or 'csv' (default: json)
     - chartConfig (optional) [object]: Chart configuration options
     - page (optional) [['integer', 'null']]: Page number for pagination (1-indexed, requires pageSize)
     - pageSize (optional) [['integer', 'null']]: Number of results per page (1-10000, required when using page)

11. list_kusto_queries
   Description: List available Kusto query templates with their parameters and metadata.
   Parameters:
     - category (optional) [string]: Filter by category (e.g., 'heartbeat', 'node', 'logs')
     - search (optional) [string]: Search term to filter queries by name or description
     - includeInternal (optional) [['boolean', 'null']]: Include internal queries (default: false)
     - includeParameters (optional) [['boolean', 'null']]: Include parameter definitions (default: false)
     - groupBy (optional) [string]: Group results by 'category', 'function', or 'none' (default: 'none')

12. get_incident_context
   Description: Get comprehensive context for an ICM incident including current state, history, comments, and metadata.
   Parameters:
     - incidentId (required) [integer]: ICM incident ID (e.g., 698227509)
     - includeHistory (optional) [['boolean', 'null']]: Include full incident history (default: true)
     - includeComments (optional) [['boolean', 'null']]: Include all comments (default: true)
     - includeDescription (optional) [['boolean', 'null']]: Include incident description (default: true)
     - maxHistoryItems (optional) [['integer', 'null']]: Maximum history items to return (default: 100)

13. create_incident_folder
   Description: Create a working folder for incident investigation with templates and structure.
   Parameters:
     - incidentId (required) [string]: ICM incident ID
     - allowExisting (optional) [['boolean', 'null']]: Allow using existing folder (default: true)
     - customPath (optional) [string]: Custom base path for incident folders (optional)
     - autoPopulateContext (optional) [['boolean', 'null']]: Automatically fetch incident context (default: false)

14. get_task_catalog
   Description: Get catalog of all registered workflow tasks with optional JSON Schema.
   Parameters:
     - format (optional) [string]: Output format: 'list' or 'detailed' (default: 'list')
     - includeSchema (optional) [['boolean', 'null']]: Include JSON Schema for task validation (default: false)

15. list_workflows
   Description: List available workflow templates with metadata.
   Parameters:
     - includeInternal (optional) [['boolean', 'null']]: Include internal workflows (default: false)

================================================================================
SUMMARY
================================================================================

Tools specifically requested:
  ✓ resolve_to_node_id: FOUND
  ✓ create_incident_folder: FOUND

Tools related to incident investigation:
  • run_batch_kusto_query
  • get_query_schema
  • check_kusto_health
  • get_current_investigation
  • resolve_to_node_id
  • run_kusto_query
  • list_kusto_queries
  • get_incident_context
  • create_incident_folder

================================================================================