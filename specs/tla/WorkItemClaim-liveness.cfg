\* TLC liveness model configuration for WorkItemClaim
\* Run: tlc WorkItemClaim.tla -config WorkItemClaim-liveness.cfg

CONSTANTS
    Agents = {a1, a2}
    Items = {i1, i2}

SPECIFICATION FairSpec

\* Safety invariants (still checked)
INVARIANT TypeOK
INVARIANT NoDuplicateClaim
INVARIANT LockExclusion
INVARIANT ClaimImpliesOwnership
INVARIANT ActiveSessionOwned
INVARIANT JournalPrecedence

\* Liveness properties
PROPERTY ClaimLiveness
