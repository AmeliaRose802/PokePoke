# PokePoke Dependencies
# Python 3.12+ required (see pyproject.toml)

# GitHub Copilot SDK for programmatic CLI integration
github-copilot-sdk>=0.1.0

# Configuration
pyyaml>=6.0
dacite>=1.8.0

# Desktop GUI (native window via Edge WebView2)
pywebview>=5.0

# Cross-process file locking
filelock>=3.12

# Process monitoring for subprocess output capture
psutil>=5.9.0

# OpenTelemetry for structured observability (opt-in via config)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
